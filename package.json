{
  "name": "cards",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.12.2",
  "scripts": {
    "dev:server": "pnpm --filter server dev",
    "dev:client": "pnpm --filter client dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}
